# Copy to .dev.vars for local development.
HASH_SECRET=local-dev-secret
TAP_BASE=http://localhost:8787
# Empty = localhost serves taps, /admin and /app together. Production splits go / admin / dashboard.tap4.ph.
ADMIN_HOST=
APP_HOST=
