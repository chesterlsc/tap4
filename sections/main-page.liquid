<section class="sec sec--tight">
  <div class="sec__titles"><h1 class="h2">{{ page.title | escape }}</h1></div>
  <div class="rte narrow">{{ page.content }}</div>
</section>

{% schema %}
{ "name": "Page", "settings": [] }
{% endschema %}
